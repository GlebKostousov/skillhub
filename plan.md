# Текущий план SkillHub

Дата: 2026-09-12
Режим: E2 принят

## Сейчас

Эпик E2 «Автовыбор режима и обычные ответы» принят. Главная — окно ассистента:
намерение, материал, бейдж выбранного режима и список пяти режимов только для
чтения. `POST /api/assistant` принимает UTF-8 JSON, проверяет CSRF и точный
`Origin`, снимок скиллов берёт только из `SkillRegistry.capture()`.

Параллельный эпик E3 владеет `protocol`, Word и обработчиком `meeting-protocol`.
Пока E3 не зарегистрирует обработчик, выбор протокола показывает бейдж и исход
`handler_unavailable`.

## Следующий шаг

Завершить параллельный эпик E3 «Протокол встречи и Word».

## Активный эпик

E3. E5 зависит от принятого E2 и открывается только по явной команде владельца.

## Правило движения

- Один слайс должен заканчиваться наблюдаемым результатом и собственной проверкой.
- Следующий слайс не начинается, пока текущий не принят.
- Без явной команды владельца код не писать.
- Текущий next-step хранится только в этом файле.
- Порог покрытия: 85 %.
