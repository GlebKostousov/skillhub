# Выжимка
Роль: ux-client
Вердикт: GATE_OK
Находки: 0
Разделение server/error boundaries в дельте `cdf166e..96d417f` не регрессировало пользовательские HTTP-контракты, request ID, root, skip-link и документированный запуск.

# Отчёт
Рецепт: да

Проверены договор E1-S1 (`docs/phases/orchestrator-e1.md:42-96`), прошлый UX rework-1 и UX-поверхность дельты: `README.md`, `web/errors.py`, `web/_boundaries.py` и их composition root. Выполнены прямые process/HTTP-пробы без изменения code/tests.

## flow

- README оставляет одну явную команду запуска: `uv run python -m skillhub --port 8000`; фактический процесс стартует на loopback и обслуживает root/health.
- Root по-прежнему показывает единственный статус «Приложение работает» без дублирующего текста.
- `/health` остаётся прямой проверкой готовности с ответом `{"status":"ok"}`.
- Неизвестный маршрут, неподдерживаемый метод и недоверенный Host завершаются понятным русским отказом в одном JSON-формате.

## states

- Success: root и `/health` доступны после одной документированной команды и имеют однозначные состояния готовности.
- Error: 400, 404, 405 и 500 сохраняют русский JSON-envelope; 405 также сохраняет `Allow: GET`.
- Loading, empty, partial: N/A — в E1-S1 нет асинхронной загрузки, пользовательского набора данных или частичной выдачи.
- Offline: N/A — клиентского runtime нет; недоступность локального процесса отображает браузер.

## errors/API

- Единый формат отказа сохранён после разделения файлов: `{"error":{"code":"…","message":"…"}}`.
- 400 Host: `bad_request` / «Некорректный запрос.»; 404: `not_found` / «Запрошенные данные не найдены.»; 405: `method_not_allowed` / «Метод запроса не поддерживается.».
- Искусственно вызванный unexpected error вернул 500 с `internal_error` / «Внутренняя ошибка сервера.» без значения исключения.
- Root, health, 400, 404, 405 и 500 получают непустой `X-Request-ID`; для 500 ID ответа совпал с ID события ошибки.
- Неверный `--port` завершается кодом 2 и одним машинно понятным событием `startup.cli_error` / `invalid_arguments`, без usage, исходного argv и traceback. Команда и допустимый способ запуска в README остаются однозначными.

## a11y

- UI-шаблон и статика в дельте не менялись; подтверждённый в rework-1 клавиатурный flow не затронут.
- Текущий root сохраняет `lang="ru"`, один `<main>`, один `<h1>`, именованный регион и единственный статус.
- Skip-link остаётся первой фокусируемой ссылкой на `#content`, а цель — `MAIN#content` с `tabindex="-1"`; JavaScript на странице отсутствует.
- Root-copy «Приложение работает» остаётся единственным пользовательским сообщением состояния.

## rule 22

N/A — в E1-S1 нет форм, полей ввода, отправки или сохранения пользовательских данных, поэтому сохранение значений и ошибки полей неприменимы.

## findings

- P0–P3: нет.
