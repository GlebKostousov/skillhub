# Выжимка

Роль: qa-tester
Scope: `e986218..4cd71a6`
Вердикт: **GATE_OK**
Findings: 0 — P0: 0, P1: 0, P2: 0, P3: 0.

Rework закрывает `SEC-E1S3-001`: публичный конструктор принимает только
`root` и отклоняет `_loader`, а `reload()` обращается прямо к приватной
файловой границе S2. Переопределение или замена публичного `load()` не
позволяет передать произвольный `LoadReport` в публикацию.

Сериализация reload, четыре lock-free читателя, атомарная замена снимка,
непубликующий `load()`, неизменяемость старых ссылок и детерминированный
Unicode-поиск сохранились. Финальный результат: 205 passed, 4 ожидаемых
platform skip, branch coverage 89,19 % при пороге 85 %. Покрытие применялось
только как gate.

# Отчёт

Рецепт: да.

## Договор, предыдущие проверки и delta

Проверены договор E1-S3 из `docs/phases/orchestrator-e1.md`, `CONTEXT.md`,
исходные handoff QA/security S3 и committed delta `e986218..4cd71a6`.
Диапазон содержит один коммит
`4cd71a6 fix(registry): закрыть обход загрузчика`; merge-base равен
`e986218`.

Delta удаляет `_loader` из `SkillRegistry.__init__`, переносит штатное чтение
в приватную `_load_registry(root)` и вызывает эту функцию напрямую из
`load()` и `reload()`. Конкурентные тесты управляют приватной функцией через
test-only monkeypatch вместо публичного production-параметра.

QA изменил только `tests/test_registry_snapshot.py` и этот handoff.
Production-файлы, зависимости, lock-файл и commit не менялись.

## Матрица рисков

- P0, внешняя или частичная публикация: mapping и frozen `Skill` остаются
  неизменяемыми, один reload заменяет снимок один раз, старые ссылки сохраняют
  прежние значения — пройдено.
- P1, обход S2 через конструктор: публичная сигнатура содержит только `root`,
  вызов с `_loader` завершается `TypeError` — пройдено.
- P1, публикация произвольного отчёта: subclass override и monkeypatch
  публичного `load()` возвращают отчёт с повторным именем и непроверенным
  содержимым, но `reload()` публикует только результат приватной файловой
  загрузки — пройдено.
- P1, конкурентная целостность: два reload остаются сериализованными с
  `max_active == 1`; четыре читателя видят только целые старый или новый
  снимки — пройдено.
- P2, лишняя публикация: отдельные `load()` и `search()` не меняют ссылку на
  снимок; один reload выполняет ровно одну замену — пройдено.
- P2, поиск и порядок: `casefold()` для name/caption, несколько совпадений,
  пустой запрос, отсутствие совпадений и канонический порядок по name
  сохранились — пройдено.
- P2, файловые исходы S2: пять валидных соседей с битым шестым,
  missing/empty root и повтор неизменённого дерева возвращают прежние точные
  loaded/skipped и снимки — пройдено.
- P3, публичная поверхность: альтернативного источника отчёта в сигнатуре
  конструктора нет, а публичного метода публикации `LoadReport` не добавлено
  — пройдено.
- Регрессия полного набора и coverage gate 85 % — пройдено.

## Mutant audit

Mutants применялись только в памяти отдельных Python-процессов; production на
диске не изменялся.

- Mutant, снова принимающий `_loader`, убит behavioral oracle отклонения
  аргумента и новым oracle точной публичной сигнатуры.
- Mutant `reload()` через `SkillRegistry.load(self)` убит новым тестом,
  заменяющим публичный метод на источник malicious report.
- Mutant `reload()` через `self.load()` остаётся убит subclass override с
  повторным именем и непроверенным содержимым.
- Mutant, публикующий из обычного `load()`, остаётся убит проверкой пустого
  снимка после загрузки и проверкой сохранения заполненной старой ссылки.
- Прежние mutants без reload-lock, с двойной/промежуточной публикацией,
  `lower()` вместо `casefold()` и сортировкой поиска по caption остаются убиты
  существующими риск-ориентированными тестами.

Все четыре выполненных in-memory mutation runs завершились ожидаемыми
отказами оракулов. Живых mutants в заявленных рисках не обнаружено.

## Аудит тестов

- Existing constructor test доказывал runtime-отказ `_loader`, но не фиксировал
  отсутствие альтернативного источника в публичной сигнатуре. Добавлен
  точный oracle `("root",)`.
- Existing subclass oracle убивал динамический вызов `self.load()`, но mutant
  с явным `SkillRegistry.load(self)` мог сохранить зелёный результат.
  Добавлена замена метода на уровне публичного класса; `reload()` обязан её
  игнорировать и загрузить валидный файл через S2.
- Malicious report содержит повторное имя и непроверенные caption,
  description и body. Он наблюдаем через отдельный публичный `load()`, но не
  попадает в snapshot.
- Конкурентные тесты используют `Event` и `Barrier`, а не `sleep`; timeout
  остаётся только защитой от зависания. Два concurrency-теста прошли 25 раз
  подряд.
- Новые тесты выбраны по риску обхода trust boundary, а не для увеличения
  процента покрытия.

## Команды и результаты

- Baseline `uv run --no-sync pytest -q -rs` → 203 passed, 4 skipped,
  branch coverage 89,19 %.
- Focus после QA-изменений:
  `uv run --no-sync pytest -q tests/test_registry_snapshot.py --no-cov`
  → 19 passed.
- Повтор concurrency focus 25 раз → 25/25 успешно.
- In-memory mutation audit → 4/4 mutants убиты.
- Финальный `uv run --no-sync pytest -q -rs` → 205 passed, 4 skipped,
  branch coverage 89,19 %, threshold 85 % достигнут.
- `uv run --no-sync ruff check .` → успешно.
- `uv run --no-sync ruff format --check .` → успешно, 119 файлов.
- `uv run --no-sync mypy src tests` → успешно, 41 source file.
- `uv run --no-sync lint-imports` → 1 контракт соблюдён.
- `uv run --no-sync ruff check --select C90 --config
  "lint.mccabe.max-complexity=3" src/skillhub/registry` → успешно.
- `git diff --check e986218..4cd71a6` и `git diff --check` → успешно.

## Findings

P0: 0.
P1: 0.
P2: 0.
P3: 0.

**GATE_OK**
