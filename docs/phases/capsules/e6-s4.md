# Capsule E6-S4 — демонстрация и защита

Исход: короткий повторяемый сценарий пяти режимов, protocol→DOCX, usage и сводка eval; каждый тезис имеет тест, метрику или документированное ограничение.

Зависимости: E6-S1, E6-S2, E6-S3.

Приёмка:

1. `eval/run_demo.py --offline` повторяется без ручной починки фикстур.
2. Архитектура §7d/§10 и threat model совпадают с as-built S1–S3.
3. README получает только разделы демонстрации и ограничений; команды CI/Docker S3 не переписываются скрыто.
4. Новые пользовательские маршруты не появляются.

Риски: happy, deny, invalid, domain. Replay: N/A. Attack: закрыт в S1/S2. Concurrency: N/A.

Claims: `eval/run_demo.py`, `docs/architecture/skillhub.md`, `docs/architecture/decision-rationale.md`, `docs/security/threat-model.md`, `docs/phases/epic-e6-security-delivery.md`, `docs/phases/skillhub-program.md`, `CONTEXT.md`, `plan.md`; `demo.five-modes-smoke`, `demo.protocol-docx`, `demo.usage-budget`, `docs.security-as-built`, `docs.limitations-not-immunity`.

Запрещено: новые режимы, CLI, хранение транскрипций, ручной выбор скилла, CRUD тарифов, обещание иммунитета к injection.
