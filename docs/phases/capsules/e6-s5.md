# Capsule E6-S5 — форматирование для CI gate

Исход: `ruff format --check .` завершается 0, чтобы CI из E6-S3 мог быть зелёным.

Зависимости: нет. Разблокирует rework E6-S3 finding UR-E6-S3-01.

Приёмка:

1. Пять уже принятых файлов с дрейфом формата приведены к ruff.
2. Поведение и тесты не меняются, только форматирование.
3. `uv run --no-sync ruff format --check .` exit 0.

Риски: happy. Deny/invalid/attack/replay/concurrency: N/A — механический format.

Claims: `src/skillhub/docx_export/_builder.py`, `src/skillhub/protocol/_verify.py`, `tests/test_docx_export.py`, `tests/test_protocol_roundtrip.py`, `tests/test_protocol_verify.py`; `ci.ruff-format-lock`.

Запрещено: менять логику, Docker, CI YAML, любые другие файлы.
