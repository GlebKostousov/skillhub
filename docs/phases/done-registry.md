# Реестр сделанного

| ID | Решение | Мотивация | Грабли | Ссылка |
|---|---|---|---|---|
| E1 | Принят каркас, безопасный файловый реестр, атомарный снимок и каталог пяти режимов в API/UI | Первый вертикальный путь без модели: запуск, чтение скиллов, поиск и reload | Порог покрытия 85 %; handoff-простыни — в `docs/handoff/archive/` | `docs/phases/orchestrator-e1.md`, `docs/phases/epic-e1-foundation-registry.md` |
| E2 | Приняты шлюз DeepSeek, классификатор, текстовые ответы четырёх простых режимов и окно ассистента | Пользователь пишет намерение и материал; режим выбирается сам; без ключа старт живой | До регистрации обработчика E3 выбор протокола давал `handler_unavailable`; живой порог матрицы 95% без ключа не красный | `docs/phases/orchestrator-e2.md`, `docs/phases/epic-e2-classifier-assistant.md` |
| E3 | Приняты грамматика протокола, parse/render, порт черновика, Word и страница `/protocol` | Готовый Markdown проверяется и скачивается как `protocol.docx`; ассистент собирает черновик через `create_draft` | Четыре H2 обязательны; `|` в ячейке запрещён; без ключа кнопка черновика на `/protocol` выключена | `docs/phases/orchestrator-e3.md`, `docs/phases/epic-e3-protocol-docx.md` |
| E4 | Приняты детерминированные уточнения, таблица решений, `finalize` без модели и `verify` по маркерам | Пропуски становятся вопросами на `/protocol`; pending не меняет черновик; обсуждение и «решили не X» не подтверждают пункт | Идентификаторы пересчитывает `build_clarifications(parse(text))`; серверной сессии нет; LLM-сигнал §7c вне E4 | `docs/phases/orchestrator-e4.md`, `docs/phases/epic-e4-clarifications.md` |
| E5 | Приняты тарифы YAML, SQLite-журнал, дневной резерв и экран `/usage` | Пользователь видит нано-USD запроса, день и историю; лимит останавливает вызов до HTTP; текст встречи в журнал не пишется | Резерв сериализуется `BEGIN IMMEDIATE`; фактический `cost_nanos` режется до дневного потолка; зависший reserve снимается при открытии | `docs/phases/orchestrator-e5.md`, `docs/phases/epic-e5-usage-observability.md` |
| E6 | Приняты атакующие тесты, офлайн-корпус injection, CI, Docker и офлайн-демо | Локальный и CI gate зелёный; пять режимов различаются; Word собирается безопасно; cold start по README | Живой вызов модели не merge-gate; абсолютная защита от injection не обещается; demo — локальная проверка, не job CI | `docs/phases/orchestrator-e6.md`, `docs/phases/epic-e6-security-delivery.md` |
| settings-hot-reload | Приняты страница «Настройки», overlay без секретов, горячая замена снимка | Оператор меняет живые поля без перезапуска; ключ поставщика на страницу не выходит | Посев `max_tokens` режется потолком тарифа; env-бюджет только до overlay | `docs/phases/orchestrator-settings-hot-reload.md` |
